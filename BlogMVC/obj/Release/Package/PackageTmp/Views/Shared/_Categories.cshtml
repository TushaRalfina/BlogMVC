@model IEnumerable<BlogMVC.Models.category>



@foreach (var category in Model)
{


    if (category.parent_id == null)
{
    <li>
        <a href="@Url.Action("Categories", "Home", new { category_id = category.id })">

            @category.name
           
        </a>
    </li>
    }
}

